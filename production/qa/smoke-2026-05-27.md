## Smoke Check Report
**Date**: 2026-05-27
**Sprint**: Sprint 01
**Engine**: Godot 4.6.3
**QA Plan**: `production/qa/qa-plan-sprint-01-2026-05-27.md`
**Argument**: sprint

---

### Automated Tests

**Status**: PASS (70 tests, 70 passing, 769 assertions)

Command:

```bash
godot --headless --import --path . && godot --headless --path . -s res://addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gdir=res://tests/integration -ginclude_subdirs -gexit
```

Notes:
- Project import completed under Godot 4.6.3.
- CI workflow exists at `.github/workflows/tests.yml`.
- Godot reports the nested vertical-slice `project.godot` folder is ignored during root import; this is expected for the current repository layout.
- Godot reports an ObjectDB leak warning at import exit; no test failures or runtime parser failures were observed.

---

### Test Coverage

| Story | Type | Test File / Evidence | Coverage Status |
|-------|------|----------------------|----------------|
| SaveData Resource | Logic | `tests/unit/save/test_save_data_resource.gd` | COVERED |
| Save Transaction Commit And Rollback | Integration | `tests/integration/save/test_save_transaction_commit_rollback.gd` | COVERED |
| Commit Signals And Failure Hooks | Integration | `tests/integration/save/test_commit_signals_and_failure_hooks.gd` | COVERED |
| Ending ID Load Projection | Integration | `tests/integration/save/test_ending_id_load_projection.gd` | COVERED |
| Semantic Action Router | Logic | `tests/unit/input/test_semantic_action_router.gd` | COVERED |
| D-Pad Confirm Cancel Flows | UI | `production/qa/evidence/input-dpad-confirm-cancel-evidence.md` | MANUAL/EVIDENCE |
| Godot 4.6 Dual Focus | UI | `production/qa/evidence/input-dual-focus-evidence.md` | MANUAL/EVIDENCE |
| Contextual Counter Routing | Integration | `tests/integration/input/test_contextual_counter_routing.gd` | COVERED |
| Content Registry Validation | Logic | `tests/unit/content/test_content_registry_validation.gd` | COVERED |
| Scene Flow Safe Transitions | Integration | `tests/integration/scene_flow/test_scene_flow_safe_transitions.gd` | COVERED |
| Bootstrap Service Order | Integration | `tests/integration/bootstrap/test_bootstrap_service_order.gd` | COVERED |
| Semantic Event Contract Harness | Integration | `tests/integration/events/test_semantic_event_contract_harness.gd` | COVERED |

**Summary**: 10 automated-covered, 2 manual/evidence, 0 missing.

---

### Manual Smoke Checks

- [x] Godot project imports without parser/runtime errors that block tests — PASS
- [x] GUT unit and integration suites run from the project root — PASS
- [x] SaveData default slot can be saved and loaded without data loss — PASS via automated tests
- [x] Save transaction failure injection preserves canonical save data — PASS via automated tests
- [x] InputRouter emits semantic actions without raw hardware details crossing into feature consumers — PASS via automated tests
- [x] Content validation reports duplicate and missing required IDs — PASS via automated tests
- [x] SceneFlow preserves the active screen when transition setup fails — PASS via automated tests
- [-] Full production runtime launch/main-menu smoke — N/A for Sprint 01 Foundation services; production shell/main menu is not in scope for this sprint.
- [-] Frame-rate/performance smoke — N/A for Sprint 01 Foundation service harnesses; no player-facing runtime scene is under test.

---

### Missing Test Evidence

All Logic and Integration stories have test coverage. Both UI stories have approved evidence docs.

---

### Verdict: PASS WITH WARNINGS

Sprint 01 is ready for QA sign-off. The warning is limited to runtime/player-facing smoke checks being out of scope because this sprint delivered Foundation services and harnesses rather than a production main menu or playable runtime shell.
