name: Nextgen Stone guardian
on:
  pull_request:
    paths:
      - 'dragon-forge-nextgen/**'
      - '.github/workflows/stone-guardian-ci.yml'
      - '.stone-transfer/**'
permissions:
  contents: read
jobs:
  placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Stone source recovery in progress"
