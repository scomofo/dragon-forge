node_modules/
.superpowers/
dist/
.claude/worktrees/
.playtest-artifacts/
# Godot generated cache (rebuild on first open)
dragon-forge-godot/.godot/
# Temporary GUT clone used during setup
.gut_tmp/
npm-debug.log
# Repo-root art master is no longer tracked. public/assets/ is the tracked web
# source of truth; dragon-forge-godot/assets/ is the tracked Godot copy. The
# assets/ tree is local scratch for art generators.
/assets/

# Synthesis art drop — raw ChatGPT originals are scratch; shipped copies live
# in public/assets/dragons/ + dragon-forge-godot/assets/dragons/.
handoff/synthesis_incoming/*.png

# Game Studios agents' per-agent scratch memory — working state, not game code.
.claude/agent-memory/
# Local preview-server config for the Claude Preview MCP tool.
.claude/launch.json
